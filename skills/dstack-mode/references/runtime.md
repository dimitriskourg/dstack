# Runtime selection

Before delegation, read the installed dstack capability contract and host-selection contract under `DSTACK_HOME/contracts/`, then read the selected adapter. Reconcile documented support with the operations visible in the current session.

Parent execution is the universal fallback. Never invent helper parameters, model identifiers, isolation, persistence, or wake behavior.
