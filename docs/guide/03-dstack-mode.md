# dstack mode

Call the Skill tool with `dstack-mode` for the full pstack-derived router and playbooks. The router keeps concise writing, deliberate delegation, simple code, and verification while removing provider task syntax.

The skill chooses a playbook, loads applicable principle skills, uses configured profiles for helpers, and keeps the parent responsible for every delegated result.
